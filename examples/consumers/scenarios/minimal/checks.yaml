cases:
  - id: catalog_has_asset_kinds
    artifact: catalog
    expect_node_kinds:
      - table
      - column
  - id: catalog_has_orders_base_amount
    artifact: catalog
    expect_node_exists:
      - column:orders_base.amount
  - id: impact_selection_resolves
    impact: orders_base.amount_upstream
    expect_selection_id: column:orders_base.amount
  - id: impact_reports_warehouse_bind_unresolved
    impact: orders_base.amount_upstream
    expect_warning_code: warehouse_bind_unresolved
