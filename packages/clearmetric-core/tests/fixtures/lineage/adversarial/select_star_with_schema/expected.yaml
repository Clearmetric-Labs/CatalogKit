dialect: postgres
mode: exact_edges
derives_from:
  - [column:report.id, column:raw_orders.id]
  - [column:report.amount, column:raw_orders.amount]
warnings:
  - {code: select_star_expanded, dataset: report}
